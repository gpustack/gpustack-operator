// Code generated by api. DO NOT EDIT.

package v1alpha1

// DeviceFabricApplyConfiguration represents a declarative configuration of the DeviceFabric type for use
// with apply.
//
// DeviceFabric describes the scale-up interconnect domain a device belongs to — the set of
// accelerators that can address one another directly over the manufacturer's own interconnect,
// which on some generations spans machines.
//
// It is recorded PER DEVICE and never as a group- or worker-level field, because the domain
// CROSS-CUTS both: NVIDIA reports a clique per GPU and one machine can hold several, and AMD's
// hive id is likewise read per GPU. So "same model, same machine, different domain" is
// reachable, and a coarser field would flatten it. The domain ITSELF is a DERIVED aggregation —
// the devices sharing Kind and ID — which is the same rule DevicesSpec.Interfaces states for
// relating an interface to an accelerator: publish comparable coordinates, never a stored
// cross-reference. Recording a higher level's fact on each device also matches RoCE above, which
// belongs to the card rather than to the accelerator.
//
// Only the Ascend detector fills this today. The fields below say which manufacturer publishes
// each, so that an empty value is readable as "this generation has none" rather than as a hole:
// what a manufacturer's driver does not report, its detector cannot record.
//
// Not every interconnect fits. Cambricon's MLU-Link publishes no domain identity at all, only
// per-link remote information, so it describes an EDGE LIST and is not recorded here.
type DeviceFabricApplyConfiguration struct {
	// Kind names the interconnect, and is what makes ID interpretable at all: an Ascend UB super
	// pod id, an NVIDIA NVLink cluster uuid and an AMD XGMI hive id share no namespace, so two
	// devices are in one domain only when Kind AND ID both match.
	//
	// One of `ub`, `nvlink` or `xgmi`.
	Kind *string `json:"kind,omitempty"`
	// ID identifies the domain, and is comparable ACROSS WORKERS — that is the whole point of
	// publishing it, since a domain spanning machines cannot be recognized from one machine's
	// record alone.
	//
	// Ascend reports the super pod id, NVIDIA the fabric cluster uuid, AMD the XGMI hive id.
	//
	// Empty where no domain was identified — and on Ascend that includes a device whose shape is
	// not in a super pod AT ALL, even though it answered the query. The driver answers it for a
	// plain server too, since that answer is how the shape is established, so an id is published
	// only for `pod-1d` and `pod-2d`. Publishing the placeholder a standalone server reports
	// would give two unrelated machines one domain, and this field is compared across workers.
	ID *string `json:"id,omitempty"`
	// Type is the domain's shape, as a word rather than a vendor number — `pod-1d`, `pod-2d`,
	// `server-8p`, `server-16p`, `server-32p`, `card-1p`, `card-4p`.
	//
	// Ascend only. A consumer assembling a communication plan reads it to tell a super pod from
	// a plain server from a standalone card, which is what decides the plan's own shape.
	Type *string `json:"type,omitempty"`
	// CliqueID is the subset of the domain that can actually address one another, where the
	// domain is partitioned into more than one.
	//
	// NVIDIA only, and on that generation it is load-bearing rather than decorative: two GPUs
	// sharing ID but not CliqueID are in one fabric and still cannot reach each other, so a
	// consumer that compares ID alone would co-locate a job that cannot communicate.
	CliqueID *string `json:"cliqueId,omitempty"`
	// MemberCount is how many members the domain reports having. Zero means the manufacturer
	// does not report it, never a domain with no members.
	//
	// Ascend only, from the super pod's own scale. It says how large the domain is without
	// enumerating it, which is what a scheduler needs before it has seen every worker in it.
	// Published under the same rule as ID: only for a shape that is in a super pod.
	//
	// Not named Size: every protobuf message carries a generated Size() method, and a field of
	// that name collides with it.
	MemberCount *uint32 `json:"memberCount,omitempty"`
	// NodeIndex is this worker's index within the domain, as the domain numbers its machines —
	// not a Kubernetes node name and not comparable to one.
	//
	// Ascend only, from the super pod's server id. Published under the same rule as ID: only for
	// a shape that is in a super pod.
	NodeIndex *string `json:"nodeIndex,omitempty"`
	// RackID is the rack this worker sits in, as the domain numbers its racks.
	//
	// Ascend only, from the super pod's chassis id. Published under the same rule as ID: only for
	// a shape that is in a super pod.
	RackID *string `json:"rackId,omitempty"`
	// Endpoints are this device's own addresses on the fabric, in the manufacturer's own
	// encoding.
	//
	// Ascend only, where each entry is a UB endpoint identifier as 32 lowercase hex characters.
	// The bytes are published UNPARSED on purpose: the function entity, the die, the port and
	// whether an endpoint carries device-to-device traffic are all bit fields inside them, so a
	// consumer derives whichever it needs rather than this API tracking a vendor bit layout that
	// only the vendor may change.
	//
	// Ordered by construction, so two consecutive reads of unchanged hardware are byte-identical.
	Endpoints []string `json:"endpoints,omitempty"`
}

// DeviceFabricApplyConfiguration constructs a declarative configuration of the DeviceFabric type for use with
// apply.
func DeviceFabric() *DeviceFabricApplyConfiguration {
	return &DeviceFabricApplyConfiguration{}
}

// WithKind sets the Kind field in the declarative configuration to the given value
// and returns the receiver, so that objects can be built by chaining "With" function invocations.
// If called multiple times, the Kind field is set to the value of the last call.
func (b *DeviceFabricApplyConfiguration) WithKind(value string) *DeviceFabricApplyConfiguration {
	b.Kind = &value
	return b
}

// WithID sets the ID field in the declarative configuration to the given value
// and returns the receiver, so that objects can be built by chaining "With" function invocations.
// If called multiple times, the ID field is set to the value of the last call.
func (b *DeviceFabricApplyConfiguration) WithID(value string) *DeviceFabricApplyConfiguration {
	b.ID = &value
	return b
}

// WithType sets the Type field in the declarative configuration to the given value
// and returns the receiver, so that objects can be built by chaining "With" function invocations.
// If called multiple times, the Type field is set to the value of the last call.
func (b *DeviceFabricApplyConfiguration) WithType(value string) *DeviceFabricApplyConfiguration {
	b.Type = &value
	return b
}

// WithCliqueID sets the CliqueID field in the declarative configuration to the given value
// and returns the receiver, so that objects can be built by chaining "With" function invocations.
// If called multiple times, the CliqueID field is set to the value of the last call.
func (b *DeviceFabricApplyConfiguration) WithCliqueID(value string) *DeviceFabricApplyConfiguration {
	b.CliqueID = &value
	return b
}

// WithMemberCount sets the MemberCount field in the declarative configuration to the given value
// and returns the receiver, so that objects can be built by chaining "With" function invocations.
// If called multiple times, the MemberCount field is set to the value of the last call.
func (b *DeviceFabricApplyConfiguration) WithMemberCount(value uint32) *DeviceFabricApplyConfiguration {
	b.MemberCount = &value
	return b
}

// WithNodeIndex sets the NodeIndex field in the declarative configuration to the given value
// and returns the receiver, so that objects can be built by chaining "With" function invocations.
// If called multiple times, the NodeIndex field is set to the value of the last call.
func (b *DeviceFabricApplyConfiguration) WithNodeIndex(value string) *DeviceFabricApplyConfiguration {
	b.NodeIndex = &value
	return b
}

// WithRackID sets the RackID field in the declarative configuration to the given value
// and returns the receiver, so that objects can be built by chaining "With" function invocations.
// If called multiple times, the RackID field is set to the value of the last call.
func (b *DeviceFabricApplyConfiguration) WithRackID(value string) *DeviceFabricApplyConfiguration {
	b.RackID = &value
	return b
}

// WithEndpoints adds the given value to the Endpoints field in the declarative configuration
// and returns the receiver, so that objects can be build by chaining "With" function invocations.
// If called multiple times, values provided by each call will be appended to the Endpoints field.
func (b *DeviceFabricApplyConfiguration) WithEndpoints(values ...string) *DeviceFabricApplyConfiguration {
	for i := range values {
		b.Endpoints = append(b.Endpoints, values[i])
	}
	return b
}
