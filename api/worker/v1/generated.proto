// SPDX-FileCopyrightText: 2026 GPUStack, Inc.
// SPDX-License-Identifier: Apache-2.0

// Code generated by "gpustack", DO NOT EDIT.


// This file was autogenerated by go-to-protobuf. Do not edit it manually!

syntax = "proto2";

package gpustack.ai.gpustack.api.worker.v1;

import "gpustack.ai/gpustack/api/worker/v1alpha1/generated.proto";
import "k8s.io/api/core/v1/generated.proto";
import "k8s.io/apimachinery/pkg/api/resource/generated.proto";
import "k8s.io/apimachinery/pkg/apis/meta/v1/generated.proto";
import "k8s.io/apimachinery/pkg/runtime/generated.proto";
import "k8s.io/apimachinery/pkg/runtime/schema/generated.proto";

// Package-wide variables from generator "generated".
option go_package = "gpustack.ai/gpustack/api/worker/v1";

// Devices is the schema for worker.gpustack.ai.
//
// Devices proxies the v1alpha1.Devices.
//
// +genclient
// +genclient:nonNamespaced
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +k8s:apireg-gen:resource:scope="Cluster",categories=["gpustack"],shortName=["devs"],subResources=["status"]
message Devices {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  optional .gpustack.ai.gpustack.api.worker.v1alpha1.DevicesSpec spec = 2;

  optional .gpustack.ai.gpustack.api.worker.v1alpha1.DevicesStatus status = 3;
}

// DevicesList holds the list of Devices.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message DevicesList {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ListMeta metadata = 1;

  repeated Devices items = 2;
}

// Instance is the schema for worker.gpustack.ai.
//
// Underhood, an Instance is mapping to a Kubernetes Pod,
// and the Instance's name is the same as the Kubernetes Pod's name.
//
// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +k8s:apireg-gen:resource:scope="Namespaced",categories=["gpustack"],shortName=["inst"]
message Instance {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  optional InstanceSpec spec = 2;

  optional InstanceStatus status = 3;
}

// InstanceEnvVar defines the environment variable to set in the Instance.
message InstanceEnvVar {
  // Name is the name of the environment variable,
  // each name in one Instance must be unique.
  optional string name = 1;

  // Value is the value of the environment variable.
  optional string value = 2;
}

// InstanceEphemeralVolume defines the ephemeral volume to mount in the Instance.
message InstanceEphemeralVolume {
  // Capacity is the size limit of the volume.
  //
  // +required
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity capacity = 1;
}

// InstanceEvents is the events of an Instance subresource,
// which provides the events related to the underlying Kubernetes Pod.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstanceEvents {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  repeated .k8s.io.api.core.v1.Event items = 2;
}

// InstanceImagePullSecret is the schema for worker.gpustack.ai.
//
// Underhood, an InstanceImagePullSecret is mapping to a Kubernetes Secret,
// and the InstanceImagePullSecret's name is the same as the Kubernetes Secret's name.
//
// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +k8s:apireg-gen:resource:scope="Namespaced",categories=["gpustack"]
message InstanceImagePullSecret {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  optional InstanceImagePullSecretSpec spec = 2;
}

// InstanceImagePullSecretList holds the list of InstanceImagePullSecret.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstanceImagePullSecretList {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ListMeta metadata = 1;

  repeated InstanceImagePullSecret items = 2;
}

// InstanceImagePullSecretSpec defines the desired state of InstanceImagePullSecret.
message InstanceImagePullSecretSpec {
  // DisplayName is the display name of the InstanceImagePullSecret.
  //
  // +k8s:validation:maxLength=64
  optional string displayName = 1;

  // Description is the description of the InstanceImagePullSecret.
  //
  // +k8s:validation:maxLength=1024
  optional string description = 2;

  // Registry is the registry of the image pull secret.
  //
  // Write-only input, it is required in create or update operations.
  //
  // It must be provided with Username, Password, and optionally Email in once operation.
  optional string registry = 3;

  // Username is the username of the image pull secret.
  //
  // Write-only input, it is required in create or update operations.
  //
  // It must be provided with Registry, Password, and optionally Email in once operation.
  optional string username = 4;

  // Password is the password of the image pull secret.
  //
  // Write-only input, it is required in create or update operations.
  //
  // It must be provided with Registry, Username, and optionally Email in once operation.
  optional string password = 5;

  // Email is the email of the image pull secret.
  //
  // Write-only input, it is required in create or update operations.
  //
  // It can be provided with Registry, Username, and Password in once operation.
  optional string email = 6;
}

// InstanceList holds the list of Instance.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstanceList {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ListMeta metadata = 1;

  repeated Instance items = 2;
}

// InstanceLog is the subresource of Instance for extracting logs,
// which is the same as Kubernetes PodLog.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstanceLog {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;
}

// InstanceLogOptions is the options of InstanceLog,
// which is the same as Kubernetes PodLogOptions.
//
// +k8s:conversion-gen:explicit-from=net/url.Values
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstanceLogOptions {
  // Follow the log stream of the Instance.
  optional bool follow = 1;

  // A relative time in seconds before the current time from which to show logs.
  // If this value precedes the time an Instance was started,
  // only logs since the Instance start will be returned.
  // If this value is in the future, no logs will be returned.
  // Only one of sinceSeconds or sinceTime may be specified.
  optional int64 sinceSeconds = 2;

  // An RFC3339 timestamp from which to show logs. If this value
  // precedes the time an Instance was started, only logs since the Instance start will be returned.
  // If this value is in the future, no logs will be returned.
  // Only one of sinceSeconds or sinceTime may be specified.
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.Time sinceTime = 3;

  // If true, add an RFC3339 or RFC3339Nano timestamp at the beginning of every line
  // of log output.
  optional bool timestamps = 4;

  // If set, the number of lines from the end of the logs to show. If not specified,
  // logs are shown from the creation of the container or sinceSeconds or sinceTime.
  optional int64 tailLines = 5;

  // If set, the number of bytes to read from the server before terminating the
  // log output. This may not display a complete final line of logging, and may return
  // slightly more or slightly less than the specified limit.
  optional int64 limitBytes = 6;
}

// InstancePersistentVolume is the schema for worker.gpustack.ai.
//
// Underhood, an InstancePersistentVolume is mapping to a Kubernetes PersistentVolumeClaim,
// and the InstancePersistentVolume's name is the same as the Kubernetes PersistentVolumeClaim's name.
//
// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +k8s:apireg-gen:resource:scope="Namespaced",categories=["gpustack"]
message InstancePersistentVolume {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  optional InstancePersistentVolumeSpec spec = 2;

  optional InstancePersistentVolumeStatus status = 3;
}

// InstancePersistentVolumeEvents is the events of an InstancePersistentVolume subresource,
// which provides the events related to the underlying Kubernetes PersistentVolumeClaim.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstancePersistentVolumeEvents {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  repeated .k8s.io.api.core.v1.Event items = 2;
}

// InstancePersistentVolumeList holds the list of InstancePersistentVolume.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstancePersistentVolumeList {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ListMeta metadata = 1;

  repeated InstancePersistentVolume items = 2;
}

// InstancePersistentVolumeSource defines the source of the InstancePersistentVolumeType.
message InstancePersistentVolumeSource {
  // NFS represents a volume source that is managed by an NFS server.
  optional NFSInstancePersistentVolumeSource nfs = 1;

  // S3 represents a volume source that is managed by an S3-compatible object storage service.
  optional S3InstancePersistentVolumeSource s3 = 2;
}

// InstancePersistentVolumeSpec defines the desired state of InstancePersistentVolume.
message InstancePersistentVolumeSpec {
  // DisplayName is the display name of the InstancePersistentVolume.
  //
  // +k8s:validation:maxLength=64
  optional string displayName = 1;

  // Description is the description of the InstancePersistentVolume.
  //
  // +k8s:validation:maxLength=1024
  optional string description = 2;

  // Type is the name of Kubernetes StorageClass that provisions corresponding Kubernetes PersistentVolume.
  //
  // Immutable after creation.
  optional string type = 3;

  // Capacity is the capacity of the InstanceVolume.
  //
  // Immutable after creation.
  //
  // +default="20Gi"
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity capacity = 4;

  // AccessMode is the access mode of the InstanceVolume.
  //
  // Immutable after creation.
  //
  // +k8s:validation:enum=["ReadWriteMany","ReadWriteOnce","ReadOnlyMany","ReadWriteOncePod"]
  optional string accessMode = 5;
}

// InstancePersistentVolumeStatus defines the observed state of InstancePersistentVolume.
message InstancePersistentVolumeStatus {
  // Phase is the phase of the InstanceVolume.
  optional string phase = 1;

  // Volume is the reference to the Kubernetes PersistentVolume that is bound to the InstanceVolume.
  optional .k8s.io.api.core.v1.ObjectReference volume = 2;
}

// InstancePersistentVolumeType is the schema for worker.gpustack.ai.
//
// Underhood, an InstancePersistentVolumeType is mapping to a Kubernetes StorageClass,
// and the InstancePersistentVolumeType's name is the same as the Kubernetes StorageClass's name.
//
// +genclient
// +genclient:nonNamespaced
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +k8s:apireg-gen:resource:scope="Cluster",categories=["gpustack"]
message InstancePersistentVolumeType {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  optional InstancePersistentVolumeTypeSpec spec = 2;
}

// InstancePersistentVolumeTypeList holds the list of InstancePersistentVolumeType.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstancePersistentVolumeTypeList {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ListMeta metadata = 1;

  repeated InstancePersistentVolumeType items = 2;
}

// InstancePersistentVolumeTypeSpec defines the desired state of InstancePersistentVolumeType.
message InstancePersistentVolumeTypeSpec {
  // DisplayName is the display name of the InstancePersistentVolumeType.
  //
  // +k8s:validation:maxLength=64
  optional string displayName = 1;

  // Description is the description of the InstancePersistentVolumeType.
  //
  // +k8s:validation:maxLength=1024
  optional string description = 2;

  // InstancePersistentVolumeSource is the source of the InstancePersistentVolumeType.
  optional InstancePersistentVolumeSource instancePersistentVolumeSource = 3;
}

// InstancePort defines the port to expose from the Instance.
message InstancePort {
  // Port is the port number to expose on the Instance.
  optional int32 port = 1;

  // Protocol is the protocol to use for the port.
  //
  // +k8s:validation:enum=["TCP","UDP","SCTP"]
  // +k8s:validation:default="TCP"
  optional string protocol = 2;

  // Name is the name of the port.
  //
  // +k8s:validation:maxLength=16
  optional string name = 3;
}

// InstanceResources defines the resource requirements for the Instance.
message InstanceResources {
  // CPU is the CPU resource requirement for the Instance, e.g. "4", "8".
  //
  // +required
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity cpu = 1;

  // RAM is the RAM resource requirement for the Instance, e.g. "40G", "16G".
  //
  // +required
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity ram = 2;

  // LocalStorage is the local storage resource requirement for the Instance, e.g. "100G", "500G".
  //
  // +required
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity localStorage = 3;

  // Accelerator is the accelerator resource requirement for the Instance, e.g. "1", "2".
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity accelerator = 4;
}

// InstanceSSHPublicKey is the schema for worker.gpustack.ai.
//
// Underhood, an InstanceSSHPublicKey is mapping to a Kubernetes Secret,
// and the InstanceSSHPublicKey's name is the same as the Kubernetes Secret's name.
//
// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +k8s:apireg-gen:resource:scope="Namespaced",categories=["gpustack"]
message InstanceSSHPublicKey {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  optional InstanceSSHPublicKeySpec spec = 2;
}

// InstanceSSHPublicKeyList holds the list of InstanceSSHPublicKey.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstanceSSHPublicKeyList {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ListMeta metadata = 1;

  repeated InstanceSSHPublicKey items = 2;
}

message InstanceSSHPublicKeySpec {
  // DisplayName is the display name of the InstanceSSHPublicKey.
  //
  // +k8s:validation:maxLength=64
  optional string displayName = 1;

  // Description is the description of the InstanceSSHPublicKey.
  //
  // +k8s:validation:maxLength=1024
  optional string description = 2;

  // Data is the SSH public key data.
  optional string data = 3;
}

// InstanceServicePort defines the port to expose from the container and the port to expose on the node.
message InstanceServicePort {
  optional InstancePort instancePort = 1;

  // NodePort is the port number to expose on the Kubernetes Node.
  optional int32 nodePort = 2;
}

// InstanceSpec defines the desired state of Instance.
message InstanceSpec {
  // DisplayName is the display name of the Instance.
  //
  // +k8s:validation:maxLength=64
  optional string displayName = 1;

  // Description is the description of the Instance.
  //
  // +k8s:validation:maxLength=1024
  optional string description = 2;

  // Type is the name of InstanceType that provisions corresponding resources.
  //
  // Immutable after creation.
  //
  // +required
  optional string type = 3;

  // InstanceTemplate is the template for the Instance to run.
  //
  // Immutable after creation.
  optional InstanceTemplate instanceTemplate = 4;

  // Volume is the volume to mount in the Instance.
  //
  // Immutable after creation.
  //
  // +required
  optional InstanceVolume volume = 5;

  // SSHPublicKey is the reference to the InstanceSSHPublicKey that contains the SSH public key to access the Instance.
  //
  // Immutable after creation.
  optional .k8s.io.api.core.v1.LocalObjectReference sshPublicKey = 6;
}

// InstanceStatus defines the observed state of Instance.
message InstanceStatus {
  // Phase is the current phase of the Instance.
  optional string phase = 1;

  // PhaseMessage is the message to describe the current phase of the Instance.
  optional string phaseMessage = 2;

  // HostIPs holds the IP addresses allocated to the host.
  //
  // +patchStrategy=merge
  // +patchMergeKey=ip
  // +listType=map
  // +listMapKey=ip
  repeated .k8s.io.api.core.v1.HostIP hostIPs = 3;

  // PodIPs holds the IP addresses allocated to the Kubernetes Pod that related to the Instance.
  //
  // +patchStrategy=merge
  // +patchMergeKey=ip
  // +listType=map
  // +listMapKey=ip
  repeated .k8s.io.api.core.v1.PodIP podIPs = 4;

  // Ports is the list of ports to expose from the Instance.
  //
  // +patchMergeKey=port
  // +patchStrategy=merge
  // +listType=map
  // +listMapKey=port
  // +listMapKey=protocol
  repeated InstanceServicePort ports = 5;
}

// InstanceTemplate defines the template for the Instance to run.
message InstanceTemplate {
  // Image is the container image to run.
  //
  // +required
  optional string image = 1;

  // ImagePullPolicy is the image pull policy to use.
  //
  // +default="IfNotPresent"
  // +k8s:validation:enum=["Always","IfNotPresent","Never"]
  optional string imagePullPolicy = 2;

  // Command is the command to run in the Instance,
  // which should overwrite the default command in the container image CMD.
  //
  // +listType=atomic
  repeated string command = 3;

  // Privileged indicates whether the container should run in privileged mode.
  //
  // +k8s:validation:default=false
  optional bool privileged = 4;

  // Ports is the list of ports to expose from the Instance.
  //
  // +patchMergeKey=port
  // +patchStrategy=merge
  // +listType=map
  // +listMapKey=port
  // +listMapKey=protocol
  repeated InstancePort ports = 5;

  // Env is the list of environment variables to set in the Instance.
  //
  // +patchMergeKey=name
  // +patchStrategy=merge
  // +listType=map
  // +listMapKey=name
  repeated InstanceEnvVar env = 6;

  // Resources is the resource requirements for the Instance.
  optional InstanceResources resources = 7;

  // VolumeMount is a path to mount the volume in the Instance.
  //
  // +default="/workspace"
  // +k8s:validation:pattern="^(/[^/]+)+$"
  // +k8s:validation:maxLength=1024
  optional string volumeMount = 8;

  // ImagePullSecret is the reference to the InstanceImagePullSecret that contains the credentials to pull the container image.
  optional .k8s.io.api.core.v1.LocalObjectReference imagePullSecret = 9;
}

// InstanceType is the schema for worker.gpustack.ai.
//
// Underhood, an InstanceType is mapping to a Kueue ClusterQueue,
// and the InstanceType's name is the same as the ClusterQueue's name.
//
// +genclient
// +genclient:nonNamespaced
// +genclient:onlyVerbs=get,list,watch
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +k8s:apireg-gen:resource:scope="Cluster",categories=["gpustack"],shortName=["instype"]
message InstanceType {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ObjectMeta metadata = 1;

  optional InstanceTypeSpec spec = 2;

  optional InstanceTypeStatus status = 3;
}

// InstanceTypeList holds the list of InstanceType.
//
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
message InstanceTypeList {
  optional .k8s.io.apimachinery.pkg.apis.meta.v1.ListMeta metadata = 1;

  repeated InstanceType items = 2;
}

// InstanceTypeResource describes the resource of the InstanceType.
message InstanceTypeResource {
  // OnceMaxRequest is the maximum value of the resource that can be requested once.
  //
  // This is a soft limitation. Requesting this value may result in scheduling failure.
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity onceMaxRequest = 1;

  // Remaining is the remaining requestable value of the resource.
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity remaining = 2;

  // Capacity is the total value of the resource.
  optional .k8s.io.apimachinery.pkg.api.resource.Quantity capacity = 3;
}

// InstanceTypeSpec defines the desired spec of InstanceType.
message InstanceTypeSpec {
  // Group indicates the group of the InstanceType.
  optional string group = 1;

  // Acceleratable indicates whether the InstanceType is acceleratable.
  optional bool acceleratable = 2;

  // Manufacturer is the name of the InstanceType manufacturer, e.g. "amd", "nvidia", "intel".
  optional string manufacturer = 3;

  // Product is the name of the InstanceType product, e.g. "A100", "V100", "T4".
  optional string product = 4;

  // Memory is the VRAM size of the InstanceType, e.g. "65535Mi".
  optional string memory = 5;

  // Family is the family of the InstanceType, e.g. "ampere", "volta", "turing".
  optional string family = 6;

  // ComputeCapability is the compute capability of the InstanceType, e.g. "8.0", "7.0".
  optional string computeCapability = 7;

  // Sliced indicates whether the InstanceType is sliced.
  // When Sliced is blank, that means the InstanceType is not sliced.
  optional int64 sliced = 8;
}

// InstanceTypeStatus describes the observed state of the InstanceType.
message InstanceTypeStatus {
  // Phase is the summary of conditions.
  optional string phase = 1;

  // PhaseMessage is the message of the phase.
  optional string phaseMessage = 2;

  // Accelerator is the accelerator resource of the InstanceType, e.g. "1", "4".
  optional InstanceTypeResource accelerator = 3;

  // CPU is the CPU resource of the InstanceType, e.g. "4", "8".
  optional InstanceTypeResource cpu = 4;

  // RAM is the RAM resource of the InstanceType, e.g. "40G", "16G".
  optional InstanceTypeResource ram = 5;

  // LocalStorage is the local storage resource of the InstanceType, e.g. "100G", "500G".
  optional InstanceTypeResource localStorage = 6;
}

// InstanceVolume defines the volume to mount in the Instance,
// which can be either ephemeral or persistent.
message InstanceVolume {
  // Ephemeral is the ephemeral volume to mount in the Instance.
  optional InstanceEphemeralVolume ephemeral = 1;

  // Persistent is the reference to the InstancePersistentVolume to mount in the Instance.
  optional .k8s.io.api.core.v1.LocalObjectReference persistent = 2;
}

// NFSInstancePersistentVolumeSource defines the source of NFS.
message NFSInstancePersistentVolumeSource {
  // Server is the hostname or IP address of the NFS server.
  //
  // Immutable after creation.
  //
  // +required
  optional string server = 1;

  // Share is the path of NFS share from the server.
  // For each InstancePersistentVolume,
  // a corresponding subpath will be created in the NFS share.
  //
  // Immutable after creation.
  //
  // +default="/"
  // +k8s:validation:pattern="^(/[^/]+)+$"
  // +k8s:validation:maxLength=1024
  optional string share = 2;

  // SubDirectory is the subdirectory in the NFS share for each InstancePersistentVolume.
  // If it is blank, the subdirectory will be the same as the volume ID of the InstancePersistentVolume.
  //
  // It supports a specific string or the following template variables:
  // - `${pvc.metadata.name}`: the name the corresponding PersistentVolumeClaim.
  // - `${pvc.metadata.namespace}`: the namespace of the corresponding PersistentVolumeClaim.
  // - `${pv.metadata.name}`: the name of the corresponding Kubernetes PersistentVolume.
  //
  // For example, specify `${pvc.metadata.namespace}/${pvc.metadata.name}` to create a subdirectory
  // with the namespaced name of the corresponding PersistentVolumeClaim in the NFS share
  // for each InstancePersistentVolume.
  //
  // Immutable after creation.
  optional string subDirectory = 3;

  // MountPermissions is the mounted directory permissions.
  // If it is non-zero, perform a chmod with the specified permissions after mounted.
  optional string mountPermissions = 4;

  // MountOptions is the mount options for the NFS share.
  //
  // +default=["hard","vers=4","rsize=1048576","wsize=1048576","noatime","nodiratime"]
  repeated string mountOptions = 5;
}

// S3InstancePersistentVolumeSource defines the source of S3.
message S3InstancePersistentVolumeSource {
  // Endpoint is the endpoint of the S3-compatible object storage service.
  //
  // +required
  optional string endpoint = 1;

  // Region is the region of the S3-compatible object storage service.
  // For AWS, the region is required to generate the presigned URL for the S3-compatible object storage service.
  optional string region = 2;

  // Insecure indicates whether to use insecure connection to the S3-compatible object storage service.
  // If it is true, the connection will not verify the TLS certificate of the S3-compatible object storage service.
  optional bool insecure = 3;

  // AccessKey is the access key of the S3-compatible object storage service.
  //
  // Write-only input, it is required in create or update operations.
  optional string accessKey = 4;

  // SecretKey is the secret key of the S3-compatible object storage service.
  //
  // Write-only input, it is required in create or update operations.
  optional string secretKey = 5;

  // Bucket is the bucket name in the S3-compatible object storage service.
  // If it is blank, for each InstancePersistentVolume,
  // a corresponding bucket with the same volume ID as the InstancePersistentVolume will be created in the S3-compatible object storage service.
  //
  // Immutable after creation.
  optional string bucket = 6;

  // Prefix is the prefix in the bucket for each InstancePersistentVolume.
  // If it is blank, the prefix will be the same as the volume ID of the InstancePersistentVolume.
  //
  // It supports a specific string or the following template variables:
  // - `${pvc.metadata.name}`: the name the corresponding PersistentVolumeClaim.
  // - `${pvc.metadata.namespace}`: the namespace of the corresponding PersistentVolumeClaim.
  // - `${pv.metadata.name}`: the name of the corresponding Kubernetes PersistentVolume.
  //
  // For example, specify `${pvc.metadata.namespace}/${pvc.metadata.name}` to create a prefix
  // with the namespaced name of the corresponding PersistentVolumeClaim in the bucket
  // for each InstancePersistentVolume.
  //
  // Immutable after creation.
  optional string prefix = 7;

  // Mounter is the mounter for the S3-compatible object storage service.
  // It is used to specify the mounter for [GeeseFS](https://github.com/yandex-cloud/geesefs).
  //
  // Immutable after creation.
  //
  // +default="geesefs"
  optional string mounter = 8;

  // MountOptions is the mount options for [GeeseFS](https://github.com/yandex-cloud/geesefs).
  //
  // Immutable after creation.
  //
  // Intensive writing for large files:
  //   disable CPU overhead, reduce freshening frequency, maximize parallelism,
  //   and reduce part sizes to improve writing performance for large files.
  //   ["--no-checksum","--memory-limit=4000","--max-flushers=32","--max-parallel-parts=32","--part-sizes=25"]
  //
  // Sequential reading for large files:
  //   increase read-ahead size and parallelism,
  //   and increase the memory cache limit to improve reading performance for large files.
  //   ["--read-ahead-large=200000"," --large-read-cutoff=10240","--read-ahead-parallel=40000","--memory-limit=8000"]
  //
  // Random reading for small files:
  //   decrease read-ahead size, extend metadata cache TTL,
  //   and increase the entry limit to improve reading performance for small files.
  //   ["--read-ahead-small=64","--small-read-cutoff=64","--read-ahead=1024","--stat-cache-ttl=300s","--entry-limit=200000"]
  //
  // High availability for writing:
  //   increase the number of retries and enable fsync on close to improve data durability for writing.
  //   ["--sdk-max-retries=10","--read-retry-attempts=5","--fsync-on-close","--cache=/mnt/disk-cache]
  //
  // For non-Yandex S3-compatible object storage service:
  //   ["--list-type=2","--no-specials"]
  //
  // +default=["--no-checksum","--memory-limit=4000","--max-flushers=32","--max-parallel-parts=32","--part-sizes=25","--list-type=2","--no-specials"]
  repeated string mountOptions = 9;
}

