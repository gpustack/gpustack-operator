diff --git a/templates/_helpers.tpl b/templates/_helpers.tpl
--- a/templates/_helpers.tpl
+++ b/templates/_helpers.tpl
@@ -34,7 +34,40 @@
 Container image reference. The tag defaults to the chart appVersion when unset.
 */}}
 {{- define "topograph.image" -}}
-{{- printf "%s:%s" .Values.image.repository (.Values.image.tag | default .Chart.AppVersion) -}}
+{{- $root := .root | default . -}}
+{{- $image := .image | default $root.Values.image -}}
+{{- $global := $root.Values.global | default dict -}}
+{{- $repository := $image.repository -}}
+{{- $registry := "" -}}
+{{- $segments := splitList "/" $repository -}}
+{{- if and (gt (len $segments) 1) (or (contains "." (first $segments)) (contains ":" (first $segments))) -}}
+{{- $registry = first $segments -}}
+{{- $repository = join "/" (rest $segments) -}}
+{{- end -}}
+{{- with $global.imageNamespace -}}
+{{- $repository = printf "%s/%s" . (last (splitList "/" $repository)) -}}
+{{- end -}}
+{{- with $global.imageRegistry -}}
+{{- $registry = trimSuffix "/" . -}}
+{{- end -}}
+{{- with $registry -}}
+{{- $repository = printf "%s/%s" . $repository -}}
+{{- end -}}
+{{- printf "%s:%s" $repository ($image.tag | default $root.Chart.AppVersion) -}}
 {{- end }}
+
+{{- define "topograph.imagePullPolicy" -}}
+{{- $root := .root | default . -}}
+{{- $global := $root.Values.global | default dict -}}
+{{- default .pullPolicy $global.imagePullPolicy -}}
+{{- end }}
+
+{{- define "topograph.imagePullSecrets" -}}
+{{- $root := .root | default . -}}
+{{- $global := $root.Values.global | default dict -}}
+{{- with (default $global.imagePullSecrets .secrets) -}}
+{{- toYaml . -}}
+{{- end -}}
+{{- end }}

 {{/*
diff --git a/templates/deployment.yaml b/templates/deployment.yaml
--- a/templates/deployment.yaml
+++ b/templates/deployment.yaml
@@ -32,9 +32,9 @@ spec:
         {{- end }}
         app.kubernetes.io/component: api-server
     spec:
-      {{- with .Values.imagePullSecrets }}
+      {{- with (include "topograph.imagePullSecrets" (dict "root" $ "secrets" .Values.imagePullSecrets)) }}
       imagePullSecrets:
-        {{- toYaml . | nindent 8 }}
+        {{- . | nindent 8 }}
       {{- end }}
       serviceAccountName: {{ include "topograph.serviceAccountName" . }}
       securityContext:
@@ -47,8 +47,8 @@ spec:
         - name: {{ .Chart.Name }}
           securityContext:
             {{- toYaml .Values.securityContext | nindent 12 }}
-          image: {{ include "topograph.image" . | quote }}
-          imagePullPolicy: {{ .Values.image.pullPolicy }}
+          image: {{ include "topograph.image" (dict "root" $) | quote }}
+          imagePullPolicy: {{ include "topograph.imagePullPolicy" (dict "root" $ "pullPolicy" .Values.image.pullPolicy) }}
           command:
             - /usr/local/bin/topograph
           args:
diff --git a/templates/nodeDataBroker/daemonset.yaml b/templates/nodeDataBroker/daemonset.yaml
--- a/templates/nodeDataBroker/daemonset.yaml
+++ b/templates/nodeDataBroker/daemonset.yaml
@@ -18,9 +18,9 @@ spec:
       labels:
         {{- include "nodeDataBroker.labels" . | nindent 8 }}
     spec:
-      {{- with .Values.imagePullSecrets }}
+      {{- with (include "topograph.imagePullSecrets" (dict "root" $ "secrets" .Values.imagePullSecrets)) }}
       imagePullSecrets:
-        {{- toYaml . | nindent 8 }}
+        {{- . | nindent 8 }}
       {{- end }}
       serviceAccountName: {{ include "nodeDataBroker.serviceAccountName" . }}
       securityContext:
@@ -33,8 +33,8 @@ spec:
         - name: node-data-broker
           securityContext:
             {{- toYaml .Values.nodeDataBroker.securityContext | nindent 12 }}
-          image: {{ include "topograph.image" . | quote }}
-          imagePullPolicy: {{ .Values.image.pullPolicy }}
+          image: {{ include "topograph.image" (dict "root" $) | quote }}
+          imagePullPolicy: {{ include "topograph.imagePullPolicy" (dict "root" $ "pullPolicy" .Values.image.pullPolicy) }}
           command:
             - /usr/local/bin/node-data-broker
           args:
diff --git a/templates/nodeObserver/deployment.yaml b/templates/nodeObserver/deployment.yaml
--- a/templates/nodeObserver/deployment.yaml
+++ b/templates/nodeObserver/deployment.yaml
@@ -23,9 +23,9 @@ spec:
         {{- toYaml . | nindent 8 }}
         {{- end }}
     spec:
-      {{- with .Values.imagePullSecrets }}
+      {{- with (include "topograph.imagePullSecrets" (dict "root" $ "secrets" .Values.imagePullSecrets)) }}
       imagePullSecrets:
-        {{- toYaml . | nindent 8 }}
+        {{- . | nindent 8 }}
       {{- end }}
       serviceAccountName: {{ include "nodeObserver.serviceAccountName" . }}
       securityContext:
@@ -38,8 +38,8 @@ spec:
         - name: node-observer
           securityContext:
             {{- toYaml .Values.nodeObserver.securityContext | nindent 12 }}
-          image: {{ include "topograph.image" . | quote }}
-          imagePullPolicy: {{ .Values.image.pullPolicy }}
+          image: {{ include "topograph.image" (dict "root" $) | quote }}
+          imagePullPolicy: {{ include "topograph.imagePullPolicy" (dict "root" $ "pullPolicy" .Values.image.pullPolicy) }}
           command:
             - /usr/local/bin/node-observer
           args:
diff --git a/templates/tests/test-healthz.yaml b/templates/tests/test-healthz.yaml
--- a/templates/tests/test-healthz.yaml
+++ b/templates/tests/test-healthz.yaml
@@ -11,9 +11,9 @@ spec:
     "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
 spec:
   restartPolicy: Never
-  {{- with .Values.imagePullSecrets }}
+  {{- with (include "topograph.imagePullSecrets" (dict "root" $ "secrets" .Values.imagePullSecrets)) }}
   imagePullSecrets:
-    {{- toYaml . | nindent 4 }}
+    {{- . | nindent 4 }}
   {{- end }}
   securityContext:
     {{- toYaml .Values.podSecurityContext | nindent 4 }}
@@ -21,8 +21,8 @@ spec:
     - name: healthz
       securityContext:
         {{- toYaml .Values.securityContext | nindent 8 }}
-      image: "{{ .Values.tests.image.repository | default .Values.image.repository }}:{{ .Values.tests.image.tag | default .Values.image.tag | default .Chart.AppVersion }}"
-      imagePullPolicy: {{ .Values.image.pullPolicy }}
+      image: "{{ include "topograph.image" (dict "root" $ "image" (dict "repository" (.Values.tests.image.repository | default .Values.image.repository) "tag" (.Values.tests.image.tag | default .Values.image.tag))) }}"
+      imagePullPolicy: {{ include "topograph.imagePullPolicy" (dict "root" $ "pullPolicy" .Values.image.pullPolicy) }}
       command: ["sh", "-c"]
       args:
         - |
diff --git a/templates/tests/test-metrics.yaml b/templates/tests/test-metrics.yaml
--- a/templates/tests/test-metrics.yaml
+++ b/templates/tests/test-metrics.yaml
@@ -11,9 +11,9 @@ spec:
     "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
 spec:
   restartPolicy: Never
-  {{- with .Values.imagePullSecrets }}
+  {{- with (include "topograph.imagePullSecrets" (dict "root" $ "secrets" .Values.imagePullSecrets)) }}
   imagePullSecrets:
-    {{- toYaml . | nindent 4 }}
+    {{- . | nindent 4 }}
   {{- end }}
   securityContext:
     {{- toYaml .Values.podSecurityContext | nindent 4 }}
@@ -21,8 +21,8 @@ spec:
     - name: metrics
       securityContext:
         {{- toYaml .Values.securityContext | nindent 8 }}
-      image: "{{ .Values.tests.image.repository | default .Values.image.repository }}:{{ .Values.tests.image.tag | default .Values.image.tag | default .Chart.AppVersion }}"
-      imagePullPolicy: {{ .Values.image.pullPolicy }}
+      image: "{{ include "topograph.image" (dict "root" $ "image" (dict "repository" (.Values.tests.image.repository | default .Values.image.repository) "tag" (.Values.tests.image.tag | default .Values.image.tag))) }}"
+      imagePullPolicy: {{ include "topograph.imagePullPolicy" (dict "root" $ "pullPolicy" .Values.image.pullPolicy) }}
       command: ["sh", "-c"]
       args:
         - |
